# Codexion
A C concurrency simulation where multiple coder threads compete for shared USB dongles, using POSIX threads, mutexes, condition variables, scheduling, and synchronization to prevent deadlocks, starvation, and burnout.
